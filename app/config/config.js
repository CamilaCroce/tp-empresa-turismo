module.exports = {
    PORT: 3000,    //Puerto en el que se ejecutara el servidor.
    DB: 'mongodb://localhost:3000/' //URL de conexion a la base de datos MongoDB, indicando el host y el puerto donde se encuentra la base de datos.
}